# Copy this file to `.airflow-app.env` and fill in the values you need locally.
# `make apply-airflow-secrets` creates Secret `airflow-app-secrets` in namespace `airflow`.

EXAMPLE_API_KEY=
EXAMPLE_API_SECRET=
