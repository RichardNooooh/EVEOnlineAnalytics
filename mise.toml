[tools]
# Infrastructure
opentofu = "latest"

# Python ecosystem
python = "3.14"
uv = "latest"

# Kubernetes
kubectl = "latest"
helm = "latest"

# Data engineering — dbt-core via pipx (not a standalone binary)
"pipx:dbt-core" = "latest"

# Ansible
"pipx:ansible" = "latest"
"pipx:ansible-core" = "latest"
"pipx:ansible/ansible-lint" = "latest"
"pipx:yamllint" = "latest"


# Python linting/formatting
ruff = "latest"
pre-commit = "latest"
