# Copy this file to `.ml-app.env` and fill in the non-database values you need locally.
# `make apply-ml-secrets` merges this file with Terraform-generated `.ml-db.env`
# into Secret `ml-app-secrets` in namespace `ml`.

# Optional manual fallback when Terraform is not generating `.ml-db.env`.
# MLFLOW_BACKEND_STORE_URI=postgresql://USER:PASS@HOSTNAME:5432/mlflow

EXAMPLE_API_KEY=
EXAMPLE_API_SECRET=
