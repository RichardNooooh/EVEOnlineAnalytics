# =============================================================================
# NFS Client Role — Default Variables
# =============================================================================
# Role-internal variables only. Site-specific NFS configuration
# (nfs_server, nfs_remote_path, nfs_local_mount, etc.) lives in
# group_vars/k3s_servers.yml.
# =============================================================================

---
# Pre-check timeout (seconds)
nfs_precheck_timeout: 30

# Required packages
nfs_packages:
  - nfs-common
