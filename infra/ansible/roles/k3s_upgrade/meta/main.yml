galaxy_info:
  role_name: k3s_upgrade
  namespace: eve_market
  author: Ansible Automation
  description: Rolling upgrade of k3s cluster with etcd snapshot
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  dependencies:
    - role: k3s_server
      when: false  # Soft dependency
