# Ingestion

Currently this is its own Python project; will need to refactor the dependencies once the other components are configured (i.e. `ml/` and others).

For the time being, this is its own standalone `dlt` project as well, to learn the `dlt` package and how to use it.

Source: `everef`

Destination: local `duckdb` database and/or Parquet files
