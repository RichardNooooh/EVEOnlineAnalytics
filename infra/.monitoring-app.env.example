# Copy this file to `.monitoring-app.env` and fill in the values you need locally.
# `make apply-monitoring-app-secrets` creates Secret `monitoring-app-secrets`
# in namespace `monitoring`.

EXAMPLE_API_TOKEN=
EXAMPLE_WEBHOOK_URL=
